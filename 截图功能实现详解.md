# 📸 截图功能实现详解

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    ScreenshotTool                          │
├─────────────────────────────────────────────────────────────┤
│  1. 浏览器驱动管理 (Selenium + Edge)                        │
│  2. 网页加载与截图                                          │
│  3. 长截图拼接                                              │
│  4. 图像分析与OCR识别                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、核心技术栈

| 组件 | 技术 | 作用 |
|------|------|------|
| **浏览器驱动** | Selenium + Edge WebDriver | 控制浏览器打开网页 |
| **图像处理** | PIL (Pillow) | 图像拼接、预处理 |
| **文字识别** | Tesseract OCR | 识别截图中的文字 |
| **编码格式** | Base64 | 图像数据传输 |

---

## 三、详细实现流程

### 1. 浏览器驱动初始化

```python
@classmethod
def _init_driver(cls):
    # 1. 创建浏览器配置
    edge_options = Options()
    edge_options.add_argument('--headless=new')  # 无头模式
    edge_options.add_argument('--window-size=3840,2160')  # 高分辨率
    edge_options.add_argument('--force-device-scale-factor=1.5')  # 缩放
    
    # 2. 查找或下载驱动
    driver_path = find_edge_driver()
    if driver_path:
        _driver = webdriver.Edge(service=Service(driver_path), options=edge_options)
    else:
        from webdriver_manager.microsoft import EdgeChromiumDriverManager
        _driver = webdriver.Edge(
            service=Service(EdgeChromiumDriverManager().install()),
            options=edge_options
        )
```

**关键点**：
- **无头模式**（`--headless=new`）：不显示浏览器窗口，后台运行
- **高分辨率**（3840x2160）：提高截图清晰度
- **驱动自动管理**：优先使用本地驱动，否则自动下载

### 2. 浏览器复用机制

```python
@classmethod
def _get_driver(cls):
    global _driver, _driver_lock, _driver_creation_time
    
    with _driver_lock:  # 线程安全
        if _driver is None:
            return cls._init_driver()
        
        # 检查驱动是否过期（300秒=5分钟）
        age = time.time() - _driver_creation_time
        if age > _max_driver_age:
            cls._init_driver()
        
        # 检查驱动是否可用
        try:
            _driver.current_url
            return _driver
        except Exception:
            return cls._init_driver()
```

**设计思路**：
- **单例模式**：全局只有一个浏览器实例
- **线程安全**：使用 `threading.Lock()` 保护
- **自动重连**：驱动失效时自动重新初始化

### 3. 截图主流程

```python
def _run(self, url: str, full_page: bool = False):
    # 1. 获取浏览器驱动
    driver = self._get_driver()
    
    # 2. 打开网页
    driver.get(url)
    
    # 3. 等待页面加载完成
    self._wait_for_page_load(driver)
    
    # 4. 截图
    if full_page:
        screenshot = self._capture_full_page(driver)  # 长截图
    else:
        screenshot = driver.get_screenshot_as_png()  # 普通截图
    
    # 5. 返回 Base64 编码
    return base64.b64encode(screenshot).decode('utf-8')
```

### 4. 页面加载等待

```python
def _wait_for_page_load(self, driver, timeout=10):
    start_time = time.time()
    while time.time() - start_time < timeout:
        try:
            # 检查页面是否加载完成
            ready_state = driver.execute_script("return document.readyState")
            if ready_state == "complete":
                break
        except:
            pass
        time.sleep(0.3)
    time.sleep(0.5)  # 额外等待确保渲染完成
```

### 5. 长截图实现（核心！）

```python
def _capture_full_page(self, driver):
    # 1. 滚动到页面顶部
    driver.execute_script("window.scrollTo({top: 0, left: 0, behavior: 'instant'});")
    
    # 2. 获取页面总高度和视口大小
    total_height = driver.execute_script("return Math.max(document.documentElement.scrollHeight, ...)")
    viewport_height = driver.execute_script("return window.innerHeight")
    viewport_width = driver.execute_script("return window.innerWidth")
    
    # 3. 分段截图
    screenshots = []
    current_position = 0
    overlap = 30  # 重叠区域，避免拼接缝隙
    
    while current_position < total_height:
        driver.execute_script(f"window.scrollTo(0, {current_position});")
        time.sleep(0.3)
        screenshot_part = driver.get_screenshot_as_png()
        screenshots.append(Image.open(BytesIO(screenshot_part)))
        current_position += viewport_height - overlap
    
    # 4. 拼接图片
    full_image = Image.new('RGB', (viewport_width, final_height))
    y_offset = 0
    
    for i, img in enumerate(screenshots):
        if i == 0:
            full_image.paste(img, (0, y_offset))
        else:
            crop_img = img.crop((0, overlap, img.width, img.height))
            full_image.paste(crop_img, (0, y_offset))
        y_offset += img.height - overlap
    
    # 5. 返回结果
    output_buffer = BytesIO()
    full_image.save(output_buffer, format='PNG')
    return output_buffer.getvalue()
```

**长截图原理**：
1. 计算页面总高度和视口高度
2. 循环滚动页面，每次截取当前视口
3. 使用 PIL 将多张小图拼接成完整大图
4. 处理重叠区域，避免拼接缝隙

---

## 四、图像分析与OCR

### 1. 多版本图像预处理

| 版本 | 预处理方式 | 特点 |
|------|------------|------|
| **v1** | 对比度增强 + 锐化 | 简单有效 |
| **v2** | CLAHE + 自适应二值化 | 适合低对比度场景 |
| **v3** | 灰度化 + 中值滤波 | 去噪效果好 |

### 2. OCR识别与评分

```python
def _run(self, screenshot_base64: str):
    # 1. 解码图片
    image_data = base64.b64decode(screenshot_base64)
    original_image = Image.open(BytesIO(image_data))
    
    # 2. 尝试多种预处理方案
    preprocessors = [('v1', preprocess_image_version1), ...]
    results = []
    
    for name, preprocessor in preprocessors:
        processed_img = preprocessor(original_image)
        
        # 使用Tesseract识别
        custom_config = r'--oem 3 --psm 6 -c preserve_interword_spaces=1'
        text = pytesseract.image_to_string(processed_img, lang='chi_sim+eng', config=custom_config)
        
        # 清洗和评分
        clean_text = filter_and_format_text(text)
        score = score_text(clean_text)
        results.append({'name': name, 'text': clean_text, 'score': score})
    
    # 3. 选择最佳结果
    results.sort(key=lambda x: x['score'], reverse=True)
    return results[0]['text']
```

**评分算法**：
- 中文字符比例（50%权重）
- 有效行数比例（30%权重）
- 平均行长度（20%权重）

---

## 五、完整流程图

```
用户触发截图
    ↓
ScreenshotTool._run(url, full_page)
    ↓
_get_driver() 获取浏览器实例
    ↓
driver.get(url) 打开网页
    ↓
_wait_for_page_load() 等待加载
    ↓
if full_page:
    _capture_full_page() 长截图
else:
    driver.get_screenshot_as_png() 普通截图
    ↓
base64.b64encode() 编码
    ↓
返回 Base64 字符串
    ↓
ScreenshotAnalyzerTool._run() 分析
    ↓
多版本预处理 + OCR识别
    ↓
评分选择最佳结果
    ↓
返回识别文字
```

---

## 六、技术亮点

| 特性 | 实现方式 | 优势 |
|------|----------|------|
| **浏览器复用** | 单例模式 + 线程锁 | 避免重复启动浏览器，提升性能 |
| **长截图** | 滚动拼接 + 重叠处理 | 支持任意长度页面 |
| **多版本预处理** | 尝试多种方案 | 提高OCR识别率 |
| **智能评分** | 多维度评分算法 | 选择最佳识别结果 |
| **垃圾文本过滤** | 正则匹配 + 规则过滤 | 去除OCR识别的乱码 |

---

## 七、性能优化

### 已实现的优化

1. **浏览器复用**：首次启动后复用浏览器实例
2. **并行处理**：OCR识别和LLM提取并行执行
3. **内容缓存**：相同内容不重复处理
4. **快速返回**：内容无变化时直接返回

### 性能对比

| 模式 | 首次检测 | 后续检测（无变化） | 后续检测（有变化） |
|------|----------|-------------------|------------------|
| **截图模式** | 15-25秒 | 2-5秒 | 8-15秒 |
| **普通模式** | 3-5秒 | 1-2秒 | 3-5秒 |

---

## 八、文件结构

```
src/
├── tools/
│   ├── screenshot_tools.py    # 截图工具主文件
│   │   ├── ScreenshotTool      # 截图工具类
│   │   ├── ScreenshotAnalyzerTool  # 图像分析工具类
│   │   ├── _init_driver()        # 初始化浏览器驱动
│   │   ├── _get_driver()         # 获取浏览器实例
│   │   ├── _capture_full_page()  # 长截图实现
│   │   └── preprocess_image_*()  # 图像预处理函数
│   └── ocr_tools.py           # OCR工具
└── streamlit_app.py           # 主界面
```

---

## 九、总结

截图功能的实现涉及多个技术环节：

1. **浏览器控制**：使用 Selenium 控制 Edge 浏览器
2. **页面加载**：等待 DOM 完全加载
3. **图像捕获**：普通截图或分段长截图
4. **图像处理**：多版本预处理提升识别率
5. **文字识别**：Tesseract OCR 识别中英文
6. **结果优化**：过滤垃圾文本，选择最佳结果

这种设计既保证了截图的灵活性（支持普通截图和长截图），又通过多种优化手段提高了文字识别的准确性。

---

**文件位置**：`src/tools/screenshot_tools.py`