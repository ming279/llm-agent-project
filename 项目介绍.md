# LLM Agent 网站监控系统

## 项目概述

这是一个基于 **LangChain** 框架构建的多Agent网站监控系统，具备以下核心能力：
- 自动检测网站内容变化
- 支持普通模式和截图模式双模式监控
- OCR文字识别功能
- 智能数据提取与关键词生成
- MySQL数据库持久化存储
- 历史记录管理

---

## 📁 项目架构

```
llm-agent-project/
├── src/                    # 主源码目录
│   ├── streamlit_app.py    # Streamlit Web应用界面
│   ├── main.py             # 主程序入口
│   ├── agents/             # Agent模块
│   │   ├── __init__.py
│   │   ├── supervisor_agent.py
│   │   ├── watcher_agent.py
│   │   ├── extraction_agent.py
│   │   ├── analyzer_agent.py
│   │   └── database_agent.py
│   ├── tools/              # 工具模块
│   │   ├── __init__.py
│   │   ├── web_tools.py
│   │   ├── ocr_tools.py
│   │   ├── db_tools.py
│   │   └── screenshot_tools.py
│   ├── services/           # 服务模块
│   │   ├── __init__.py
│   │   └── qwen_llm.py
│   ├── config/             # 配置模块
│   │   ├── __init__.py
│   │   └── settings.py
│   └── utils/              # 工具函数
│       ├── __init__.py
│       └── ocr_processor.py
├── .env                    # 环境变量配置
└── .venv/                  # Python虚拟环境
```

---

## 🎯 核心组件详解

### 1. Agent模块

| Agent名称 | 职责 | 核心功能 |
|-----------|------|----------|
| **SupervisorAgent** | 总协调者 | 任务分配、工具调用、结果汇总 |
| **WatcherAgent** | 网站监控 | 检测网站变化、管理历史状态 |
| **ExtractionAgent** | 数据提取 | 从网页内容提取结构化数据 |
| **AnalyzerAgent** | 变化分析 | 比较新旧内容、分析差异 |
| **StorageAgent** | 数据存储 | 将结果保存到数据库 |
| **OCRProcessor** | OCR处理 | 识别图片中的文字 |
| **DatabaseAgent** | 数据库操作 | MySQL连接、数据读写 |

### 2. 工具模块

| 工具名称 | 功能描述 | 输入输出 |
|----------|----------|----------|
| **WebScraperTool** | 网页内容抓取 | URL → JSON(文本+图片列表) |
| **ContentHasherTool** | MD5哈希计算 | 文本 → 哈希值 |
| **OCRTool** | 单张图片OCR | 图片URL → 识别文字 |
| **BatchOCRTool** | 批量图片OCR | 图片URL列表 → 识别结果列表 |
| **DatabaseTool** | 数据存储 | JSON数据 → 存储状态 |
| **QueryHistoryTool** | 历史查询 | URL → 历史记录 |
| **ScreenshotTool** | 网站截图 | URL → Base64编码图片 |
| **ScreenshotAnalyzerTool** | 截图分析 | Base64图片 → OCR识别结果 |

### 3. 服务模块

- **qwen_llm.py**：阿里云通义千问LLM API封装
- **llm_service**：全局共享的LLM服务实例

---

## 🔄 工作流程

### 普通模式
```
开始监控 → 抓取网页内容 → 计算哈希值 → 比较变化 → 提取数据 → 生成关键词 → 存储数据库 → 显示结果
```

### 截图模式
```
开始监控 → 浏览器截图 → OCR识别文字 → 计算哈希值 → 比较变化 → 提取数据 → 生成关键词 → 存储数据库 → 显示结果
```

---

## 🔧 配置说明

### 环境变量配置 (.env)

```env
# 千问API密钥
DASHSCOPE_API_KEY=your-api-key-here

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=llm_agent_db

# 默认监控配置
TARGET_WEBSITE=https://news.cctv.com/
MONITOR_INTERVAL=60
```

### 配置文件 (src/config/settings.py)

```python
DB_CONFIG = {
    'host': os.getenv('DB_HOST', 'localhost'),
    'port': int(os.getenv('DB_PORT', '3306')),
    'user': os.getenv('DB_USER', 'root'),
    'password': os.getenv('DB_PASSWORD', '123456'),
    'database': os.getenv('DB_NAME', 'llm_agent_db')
}
```

---

## 🚀 启动方式

```bash
# 进入项目目录
cd llm-agent-project

# 运行Streamlit应用
streamlit run src/streamlit_app.py
```

---

## 📋 功能特性

### 监控功能
- ✅ 网站内容变化检测
- ✅ 双模式支持（普通模式/截图模式）
- ✅ 自定义检测间隔
- ✅ 暂停/继续/停止控制

### 数据处理
- ✅ 网页内容抓取
- ✅ 结构化数据提取
- ✅ OCR文字识别
- ✅ 关键词自动生成

### 数据存储
- ✅ MySQL数据库存储
- ✅ 历史记录管理
- ✅ 按网站分类查看

### 用户界面
- ✅ 实时状态显示
- ✅ 检测结果展开查看
- ✅ 截图预览
- ✅ 历史记录时间线

---

## 🛠️ 技术栈

| 分类 | 技术 | 版本要求 |
|------|------|----------|
| 框架 | Streamlit | >=1.0 |
| LLM框架 | LangChain Core | >=0.1 |
| 浏览器自动化 | Selenium | >=4.0 |
| OCR识别 | pytesseract | >=0.3 |
| 图像处理 | Pillow | >=10.0 |
| HTML解析 | beautifulsoup4 | >=4.0 |
| HTTP请求 | requests | >=2.0 |
| MySQL驱动 | mysql-connector-python | >=8.0 |

---

## 📊 数据库设计

### 主要表结构

#### websites 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| url | VARCHAR | 网站URL |
| name | VARCHAR | 网站名称 |
| created_at | DATETIME | 创建时间 |

#### monitoring_records 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| website_id | INT | 关联网站ID |
| title | VARCHAR | 页面标题 |
| content | TEXT | 内容摘要 |
| keywords | VARCHAR | 关键词 |
| change_type | VARCHAR | 变化类型(new/updated) |
| created_at | DATETIME | 创建时间 |

#### change_history 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| website_id | INT | 关联网站ID |
| old_data | TEXT | 旧数据 |
| new_data | TEXT | 新数据 |
| changed_at | DATETIME | 变化时间 |

---

## 💡 使用说明

### 1. 环境准备
```bash
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
.venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置API密钥
在 `.env` 文件中设置千问API密钥：
```env
DASHSCOPE_API_KEY=your-api-key
```

### 3. 配置MySQL数据库
确保MySQL服务已启动，并创建数据库：
```sql
CREATE DATABASE llm_agent_db;
```

### 4. 运行应用
```bash
streamlit run src/streamlit_app.py
```

---

## 🔍 两种监控模式对比

| 对比维度 | 普通模式 | 截图模式 |
|---------|---------|---------|
| 数据来源 | 直接抓取HTML文本 | 截图后OCR识别 |
| 检测范围 | 网页文本内容 | 所有可见文字（含图片） |
| 速度 | 快（HTTP请求） | 慢（浏览器启动+OCR） |
| 资源消耗 | 低 | 高 |
| 真实性 | 受JS渲染影响 | 接近用户实际看到 |

---

## 📝 核心代码示例

### 网站监控核心逻辑

```python
def monitor_website(url, previous_hash):
    # 1. 抓取网页内容
    scraper = WebScraperTool()
    content = scraper._run(url)
    
    # 2. 计算哈希值
    hasher = ContentHasherTool()
    current_hash = hasher._run(content)
    
    # 3. 比较是否变化
    if previous_hash and current_hash == previous_hash:
        return {"changed": False}
    
    return {"changed": True, "content": content, "hash": current_hash}
```

### LLM关键词提取

```python
def _generate_keywords(text):
    prompt = f"""请从以下网页内容中提取10个最重要的关键词：
    {text[:2000]}
    关键词："""
    
    response = self.llm.invoke([HumanMessage(content=prompt)])
    keywords = [k.strip() for k in response.content.split(',') if k.strip()]
    return keywords[:10]
```

---

## 📌 项目亮点

1. **多Agent协同**：通过SupervisorAgent协调多个专业Agent完成复杂任务
2. **智能变化检测**：基于MD5哈希值比较，精准识别内容变化
3. **双模式支持**：普通模式快速检测，截图模式支持视觉检测
4. **OCR集成**：支持图片文字识别，可检测图片内容变化
5. **自动关键词生成**：基于LLM自动提取内容关键词
6. **完善的历史记录**：支持按网站分类查看历史检测记录
7. **用户友好界面**：Streamlit提供直观的可视化监控面板

---

## 📞 联系方式

如有问题或建议，请联系项目维护者。

---

*文档生成时间：2026年5月8日*