# LLM Agent 项目运行详解

## 整体架构图

```
用户界面 (Streamlit)
    ↓
┌─────────────────────────────────────┐
│         监控中心 (WatcherAgent)       │
│  ┌───────────┐  ┌────────────────┐   │
│  │ 截图模式   │  │  网页抓取模式   │   │
│  └─────┬─────┘  └──────┬───────┘   │
│        ↓                ↓            │
│  ┌─────┴─────┐  ┌──────┴───────┐   │
│  │ OCR识别   │  │ 内容哈希     │   │
│  └─────┬─────┘  └──────┬───────┘   │
│        ↓                ↓            │
│  ┌─────┴─────────────────────────┐  │
│  │      LLM 提取结构化数据         │  │
│  └─────┬─────────────────────────┘  │
│        ↓                            │
│  ┌─────┴─────────────────────────┐  │
│  │      数据库存储                 │  │
│  └────────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 详细运行流程

### 第一步：用户配置（界面操作）

用户在 Streamlit 界面上完成配置：

1. **输入监控网址**：如 `https://time.is/zh/China`
2. **设置监控间隔**：如 60 秒
3. **选择抓取模式**：
   - ✅ 开启截图模式（使用浏览器截图 + OCR）
   - ❌ 关闭截图模式（使用普通网页抓取）
4. **选择长截图**：是否滚动页面截取完整内容
5. **点击"开始监控"按钮**

---

### 第二步：初始化（程序启动）

```
Streamlit 应用启动
    ↓
init_agents() 初始化所有组件
    ↓
创建以下对象存入 session_state：
  - supervisor (SupervisorAgent)
  - watcher (WatcherAgent)
  - extractor (ExtractionAgent)
  - analyzer (AnalyzerAgent)
  - ocr (OCRProcessor)
  - db (DatabaseAgent)
  - screenshot_tool (ScreenshotTool)
  - screenshot_analyzer (ScreenshotAnalyzerTool)
```

---

### 第三步：执行检测（每次轮询）

当定时器触发或用户点击"立即检测"时，执行 `check_website_change()`：

#### 分支A：截图模式（完整流程）

```
1. 截图工具启动浏览器
   ├─ 使用 Selenium + Edge WebDriver
   ├─ 打开目标网页
   └─ 等待页面加载完成（最多30秒）

2. 获取截图
   ├─ 普通截图：截取可见区域
   └─ 长截图：滚动页面分段截取后拼接
   └─ 返回 Base64 编码的图片数据

3. OCR 识别
   ├─ 将 Base64 图片解码为 OpenCV 图像
   ├─ 图像预处理（灰度化、对比度增强、二值化）
   ├─ 使用 Tesseract OCR 识别文字
   └─ 返回识别的文本内容

4. 内容哈希检测变化
   ├─ 计算当前内容的 MD5 哈希
   ├─ 与上次哈希对比
   └─ 如果相同则跳过后续步骤

5. LLM 提取结构化数据 ⚡（调用 qwen-plus）
   ├─ 发送提示词给 LLM
   │  "请从以下网页内容中提取结构化数据..."
   ├─ LLM 返回 JSON：
   │  {
   │    "title": "页面标题",
   │    "main_content": "主要内容摘要",
   │    "key_points": ["关键点1", "关键点2"],
   │    "sentiment": "neutral"
   │  }
   └─ 存入缓存（相同内容不重复调用）

6. 分析变化
   ├─ 如果不是首次检测
   └─ 调用 LLM 对比新旧内容的差异 ⚡

7. 数据库存储
   ├─ get_or_create_website() - 获取或创建网站记录
   ├─ store_data() - 存储抓取的数据
   └─ update_last_checked() - 更新最后检查时间

8. 返回结果给界面显示
```

#### 分支B：普通抓取模式（简化流程）

```
1. 网页抓取
   ├─ 使用 requests 获取网页 HTML
   ├─ 使用 BeautifulSoup 解析 HTML
   └─ 提取文本内容和图片链接

2-8. 后续步骤与截图模式相同（跳过 OCR 步骤）
```

---

### 第四步：界面更新显示

```
检测完成 → 更新 Streamlit 界面
    ↓
显示以下信息：
  ├─ 状态信息（检测到变化 / 无变化 / 首次完成）
  ├─ 当前时间
  ├─ 检测耗时（截图、OCR、LLM、数据库各环节）
  ├─ 提取的标题
  ├─ 主要内容摘要
  ├─ 关键词
  ├─ 情感倾向
  └─ 变化分析（如果检测到变化）
```

---

### 第五步：定时轮询

```
如果监控开启：
    ↓
等待设定的间隔时间（默认60秒）
    ↓
再次执行 check_website_change()
    ↓
循环...
```

---

## 各组件职责

| 组件 | 职责 | 输入 | 输出 |
|------|------|------|------|
| **ScreenshotTool** | 浏览器截图 | URL | Base64 图片 |
| **ScreenshotAnalyzerTool** | OCR 识别 | Base64 图片 | 文本内容 |
| **WebScraperTool** | 网页抓取 | URL | HTML 内容 |
| **ContentHasherTool** | 计算哈希 | 文本内容 | MD5 哈希值 |
| **ExtractionAgent** | LLM 提取 | 文本内容 | 结构化 JSON |
| **AnalyzerAgent** | LLM 分析 | 新旧内容 | 变化分析 |
| **DatabaseAgent** | 数据库操作 | 各种数据 | 存储结果 |

---

## 性能对比

| 模式 | 首次检测 | 后续检测（无变化） | 后续检测（有变化） |
|------|----------|-------------------|------------------|
| **截图模式** | 15-25秒 | 2-5秒 | 8-15秒 |
| **普通模式** | 3-5秒 | 1-2秒 | 3-5秒 |

---

## 关键优化

1. **内容哈希缓存**：内容无变化时跳过 LLM 调用
2. **浏览器复用**：后续检测复用已有浏览器实例
3. **动态等待**：页面加载完成即继续，不固定等待时间